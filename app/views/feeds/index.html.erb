<div id="feeds">
  <%= form_for @new_feed, action: :create do |f| %>
  <p>
    <%= f.text_field :url %>
    <%= f.submit 'add' %>
  </p>
  <% end -%>
  <ul>
    <% @feeds.each do |feed| -%>
    <li class="feed"><%= link_to feed.title, feed %></li>
    <% end -%>
  </ul>
</div>
